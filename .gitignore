# Out-of-source CMake build tree
/build/

# Populated by scripts/init.sh (external tools, never vendored)
/external/

# WebAssembly build outputs from `make web` (the page is tracked, not these)
/web/vgmconv.js
/web/vgmconv.wasm
